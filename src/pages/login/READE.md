```jsx

  import { QIcon } from '@@@'

  /**
		 * 组件参数
     * 
     * @param {type} { String } [] xxxx
		 * @param {onClick} { opt => opt } 点击回调 后期删除该属性
		 * @param {className} { String } 添加额外的 className
		 * @param {size} { Number } [18] 距离
   */

  <QIcon 
    type=""
    onClick={opt => opt}
    size={}
  />

```